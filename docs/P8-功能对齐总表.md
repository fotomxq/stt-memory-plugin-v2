# P8 · V1 功能对齐总表（完全对齐的执行清单）

> 文档版本：v3.0 ｜ 日期：2026-09-26 ｜ 状态：**B8/B9 全部批次已交付并逐批发版**（B1–B7-2、B8-1~B8-7-b、B9-a~B9-d；v2.18.0~v2.32.0 共 15 个版本标签）；仅余 §7 列明的少量**非动作项**与 §6.1 的「V2 不适用」项。
> 用户要求：「完整对齐 V1 的各种功能，完整实现出来；页面尽可能的样式也对齐」
> 关联：`docs/P8b-V1面板外壳.md`（B1 交付）、V1 源码 `src/modules/11..14-UI-*.js`、`src/FTT记忆组件-v1.206.js`

---

## 1. 量级（诚实交底）

V1 的面板动作（`data-ftt-action`）去重后 **110 个**；UI 源码 **5 个模块共 5,906 行**；
设定页 **14 组子页**；另有多项依赖尚未移植的内核（快照链、跨端同步、各类修复、NSFW 弱化、情节总结、传言演化、时钟巡检、遗忘）。
因此「完全对齐」按 **9 个批次**推进，每批次交付后跑满门禁并打版本标签。

V1 UI 源码分布（行数）：

| 模块 | 行数 | 内容 |
| --- | --- | --- |
| `11-UI-样式与按钮.js` | 946 | 样式（527 行 CSS / 437 处 `#ftt-panel` 规则）、顶栏/魔杖/悬浮按钮 |
| `12-UI-编辑与面板.js` | 1438 | 面板外壳、总览、各维度编辑器、字段定义 |
| `12B-UI-关系表.js` | 435 | 通用知情关联层的关系表 UI |
| `13-UI-设置与存储开关.js` | 1351 | 设定 14 组子页、存储开关、诊断、数据管理 |
| `14-UI-渲染与操作.js` | 1301 | 列表渲染、搜索/多选、单楼分析动效、注入预览、快捷操作 |

## 2. 批次与验收

| 批次 | 目标 | V1 源 | 动作数（V1） | 验收口径 |
| --- | --- | --- | --- | --- |
| **B1 ✅** | **外壳 + 样式 + 13 分页 + 总览基础 + 维度列表/编辑器** | 11（CSS）、12（外壳/总览/编辑器） | 12 / 110 | 浮层 DOM 与 V1 同构、CSS 逐字并入、`panel.test.js` 12 项 + 冒烟 M1–M2 |
| **B2 ✅** | 条目操作与编辑器全量：字段表/新增三路径/多选批删/隐藏与速览/状态分组/搜索清理 | 11、12、14 | 15 | `docs/P8c-B2条目操作.md`；`panel.test.js` 18 项（含字段表逐维比对、批删墓碑、速览、分组删除） |
| **B3 ✅** | 提取与楼层管理：分段批量摘要、逐楼分析、进度动效、中断、清除记录 | 09、14 | 7 | `docs/P8d-B3提取与楼层.md`；`extract-batch.test.js` 9 项（分段/整段记账/失败不记账/静默补全跳过/中断/清台账/面板接线） |
| **B4-1 ✅** | 设定 **14 组子页**：框架 + **105 个配置控件**（V1 源码自动提取）+ 数据管理页导出/导入/清台账 + V2 附加设定块 | 13 | — | `docs/P8e-B4设定子页.md`；`settings-pages.test.js` 6 项（页序/控件数/键可解析/渲染/写回/面板接线） |
| B4-2（部分 ✅） | 提示词模板编辑 ✅（B6）、存储页探测/测试/同步 ✅（B7-2）；内容弱化库（B8）、调试页（B9）仍标注「待后续批次」，不放假实现 | 13 | — | 已交付部分见对应批次文档 |
| **B5 ✅** | 关系表与注入自查：关联行编辑（草稿/保存/删除/清空/扫孤儿/清推定）、按角色筛选、注入自查面板（约束段原文 + 未召回原因） | 12B、14 | 9 | `docs/P8f-B5关系表与注入自查.md`；`rel-inject.test.js` 8 项；顺带修掉 3 个真实缺陷（合并 vs replace、维度无关动作顺序、清空无效） |
| **B6 ✅** | 提示词模板编辑：33 条 / 5 组编辑、恢复默认（单条/整组/全部）、签名与 v1.132/v1.179 迁移链、破甲预设导入 | 13 | 6 | `docs/P8g-B6提示词编辑.md`；`prompts.test.js` 8 项（含 `promptSig` 逐字、迁移保留自定义、载入接线、写回持久化） |
| **B7-1 ✅** | **快照链**：内核逐字移植（建根/增量/整理/还原/清空/统计）+ 保存流水线接线 + 数据管理页区块 | 05 | 5 | `docs/P8h-B7快照链.md`；`snapshot-golden.test.js` 8 项 + **真实 V1 插件黄金样本 12**（结构指纹/还原/统计三项一致） |
| **B7-2 ✅** | **跨端同步与镜像**：原子级双向合并 + 墓碑过滤、快照链独立文件上传/并集合并、清单(meta)预判、同步日志（本机环形 + 服务端交叉合并）、两级流量门控、存储页完整分节与 5 个 V1 同名动作 | 05/06/13 | 5 | `docs/P8i-B7-2跨端同步.md`；`cross-sync-golden.test.js` 15 项（真实 V1 黄金样本 13）+ `sync-adapter.test.js` 27 项 + 冒烟 N1–N6 |
| **B8-1 ✅** | **剧情时钟域**：手工强制改写锚点（编辑/保存锁定/解锁）+ 可信锚点四级择优 + 零 AI 时间巡检（扫描/只统计/手动修复/自动巡检）+ 基础页 V1 五分节与 9 项控件补齐（含 `swForce` 强制开关） | 09/12/13/14 | 5 | `docs/P8j-B8-1时钟巡检与手工锚点.md`；`clock-patrol-golden.test.js` 23 项（真实 V1 黄金样本 19）+ 冒烟 O1–O5 |
| **B8-2 ✅** | **剧情时钟自动提取**：正文头结构（`▷`/`▶`）+ 多源择优（正文头 > 日期直取/时刻/时段词 > 自定义正则 > 标记式 > 相对推进）+ 统一解析与自动降级（日期异常 / 强制降级 / 场景兜底）+ 自动落盘与来源可解释 + 消息后 1.8s 防抖 | 09/02 | — | `docs/P8k-B8-2剧情时钟自动提取.md`；`clock-extract-golden.test.js` 12 项（真实 V1 黄金样本 27 例）+ 冒烟 P1–P5 |
| **B8-3 ✅** | **时钟域 AI 管线**：`clockRegexGen`（AI 捕捉正文 → 三条正则 + 三重校验（可编译/不匹配空串/样本有命中）+ 写回 + 试算）、`clockRepair`（AI 结合正文修复日期时间；只改日期/时间字段 + 逐条安全闸门；无可信锚点拒绝且不调用 AI） | 09/13/14 | 2 | `docs/P8l-B8-3时钟域AI管线.md`；`clock-ai-golden.test.js` 17 项（真实 V1 黄金样本 8 组）+ 冒烟 Q1–Q4 |
| **B8-4 ✅** | **内容弱化（NSFW）**：识别词条库（63 条内置 + 自定义增删改恢复）、固定规则转化库（63 条一一对应；单遍不链式 + 长词优先 + 英文词界与误伤名单）、零 AI 机械替换（镜像字段同步 + `updatedAt` 刷新）、AI 批量弱化（强校验：仍含关键词/过长一律丢弃）、分析侧提示词开关、设定「内容弱化」页四节与 10 个 V1 同名动作、总览 🌶 按钮 | 09/13/14 | 10 | `docs/P8m-B8-4内容弱化NSFW.md`；`nsfw-golden.test.js` 22 项（真实 V1 黄金样本 14 组）+ 冒烟 R1–R5 |
| **B8-5 ✅** | **遗忘域**：记忆遗忘机制（只按剧情日期 + 重要度免疫 + 保底 + 墓碑 + 3s 防抖调度，接入 `mergeDelta`）、通用遗忘清扫（六维 + 门槛/保护/叶子/保底 + 40 楼冷却闸门）、遗忘设定页 V1 五分节 + 只读诊断行；并补齐 B4 遗漏的 **20 个 `switchField` 开关**（控件 127→147，8 页顺序对齐 V1） | 09/13 | — | `docs/P8n-B8-5遗忘域.md`；`forget-golden.test.js` 17 项（真实 V1 黄金样本 8 组）+ 冒烟 S1–S4 |
| **B8-6a ✅** | **修复管线第 1 段（JS 机械清理 · 零 AI）**：内容哈希并集 + 同名称并集（字段只增不减/货币合计）+ 场景两级并集 + 状态主体归并 + 垃圾与已关闭残留清理（写墓碑）+ 衰退清扫（状态/记忆/平行 + 状态条数钳制）+ 频率与上限闸门 + 报告/日志口径 + 总览「🛠 自动修复」动作（第 2/3 段如实说明未执行） | 09/14 | 1 | `docs/P8o-B8-6a修复管线第1段.md`；`repair-golden.test.js` 13 项（真实 V1 黄金样本 7 组）+ 冒烟 T1–T3 |
| **B8-6c-1 ✅** | **相关组聚类修复（通用聚类引擎 + 记忆修复管道）**：`GROUP_REPAIR_SPECS` 四域 spec + `groupRelatedness`/`groupClusters`（标签基准共享 ≥2 标签 + 同分区 + 组规模上限防串联）/`groupPick`（游标轮询选组、缺陷条目单列、悬念孤例轮询、游标写回）+ 记忆域 `memoryMergeExact`（同正文哈希 + 同标题 ≥4 字，**不写墓碑**）/`buildMemoryRepairPrompt`（逐字符）/`applyMemoryMergeGroups`（按编号精确、跨归属拒收、删除写墓碑）/`runMemoryRepair`（六步编排，AI 后复检并合并口径）+ `retargetRelRefs`（关联行重挂，V1 怪癖原样）；长期记忆页「🔧 修复记忆」按钮 + `memoryRepair` 动作；**设定页补齐 V1 手写块 22 控件（147→169）** | 09/14 | 1 | `docs/P8r-B8-6c-1记忆聚类修复.md`；`group-repair-golden.test.js` 26 项（真实 V1 黄金样本 2 份 / 18+ 组）+ 冒烟 W1–W3 + 设定页 P2c |
| **B8-6b+ ✅** | **关联层机械维护（零 AI）**：孤儿关联行清扫（写墓碑）+ 同 `(维度,条目,角色)` 去重合并（how 可靠度排序 + 字段并集）+ 角色名按档案全名归一（`who`/`from` + 稳定 id 重算）+ 悬空引用清理（概念按名称、其余按 id、`memRefs`/`sourceRefs` 裁剪）+ `how`·偏差非法值归一 + 降级统计 + 孤儿条目三态处置（keep/clean/public，`parallels` 恒不参与）；`relMaintCounts/Touched/Summary/logRelMaint` + `mergeRelMaint`（AI 前·后两次口径合并）；接线进修复第 1 段收尾与 **AI 修订后复检** | 09/14 | 1 | `docs/P8q-B8-6b+关联维护.md`；`rel-maint-golden.test.js` 16 项（真实 V1 黄金样本 18 组）+ 冒烟 V1–V4 |
| **B8-6b ✅** | **修复管线第 2/3 段**：候选筛选（客观缺陷一律列入 + 标签组相关性高相关核对 + 中间带按比例抽查轮询 + 候选不足按相关性补足）+ 窄契约 AI 修订（字段闭集 / 硬截断 / 编号精确 / 禁止新增 / 删除写墓碑）+ 三段式编排与闸门 + 提取失败自动修复接线 | 09/14 | 1 | `docs/P8p-B8-6b修复第2-3段.md`；`repair-ai-golden.test.js` 12 项（真实 V1 黄金样本 6 组）+ 冒烟 U1–U4 |
| B8-6c+ | 剩余专用修复（概念/场景/物品/角色/计划悬念/原子）+ 状态修复；情节总结与分段总结、传言演化、平行推演与转正；世界书存储单向镜像 | 09/10/13/14 | 19+ | 逐域「内核 + 页面」成对交付；每域独立黄金样本或单测 |
| B9 | 导入导出与调试：调试日志面板（日志/缓存/重载）✅、条目瘦身与 gzip 传输（`.json.gz`）✅（B9-d）、跨端同步分歧选择 ✅（B9-d）、预设保存/载入/删除（V2 不适用，见 §6.1）、`promptPreview`（**V1 无此动作**，见 §6.1） | 01/13/14 | 7 | 导出结构与 V1 兼容（可被 V1 读回）；导入走迁移链；瘦身/gzip 与分歧选择见 `docs/P9c-B9瘦身gzip与同步分歧选择.md` |
| B8 | 高级域：时钟巡检/手工锚点、各类修复（记忆/原子/物品/概念/角色/场景/计划悬念）、NSFW 弱化（词条库+规则）、情节总结与分段总结、传言演化、平行推演与转正、遗忘清扫 | 09/10/13/14 | 31+ | 逐域「内核 + 页面」成对交付；每个域有独立黄金样本或单测 |
| B9 | 导入导出与调试：导出/导入 JSON、调试日志面板、缓存与重载、API 模型测试 | 01/13/14 | 7 | 导出结构与 V1 兼容（可被 V1 读回）；导入走迁移链 |

> 未归类动作 53 个（`selectAll`/`searchClear`/`presetSave|Load|Delete`/`rxScanTags`/`promoteParallel`/`rumorEvolve`/`snapshotClear` …）
> 分别落在 B2/B3/B5/B6/B7/B8/B9 内，实现时以「动作清单逐个打勾」的方式收尾（见 §4）。

## 3. 内核移植状态（B8/B9 的前置）

| 内核能力 | V1 位置 | 现状 |
| --- | --- | --- |
| 快照链（root/incr、上限并入、`rebuildFromSnapshots`） | `05-记忆状态与存储抽象.js` | **已移植**（B7-1，`core/snapshots.js`） |
| 跨端收敛与镜像同步（拉取合并、meta 预判、同步日志、流量门控） | `05/06` | **已移植**（B7-2，`core/cross-sync.js` + `core/sync-log.js` + `adapters/sync.js`） |
| 统一存储抽象（分类/后端/策略）与 TauriTavern 原生存储 | `05B/06` | **未移植**（V2 收敛为「本机缓冲 + 服务端记忆文件」；存储页以说明行如实呈现，见 `docs/P8i` §2） |
| 时钟巡检（`clockPatrol*`）与手工锚点锁定 | `09` + 07 | **已移植**（B8-1：`core/clock-patrol.js` + `core/clock.js` 补全） |
| 剧情时钟自动提取（正文头 / 多源择优 / 降级 / 自动落盘） | `09` + `02` | **已移植**（B8-2：`core/clock-extract.js`） |
| 时钟域 AI 管线（AI 生成正则 / AI 结合正文修复日期时间） | `09` + `13` | **已移植**（B8-3：`core/clock-ai.js`） |
| 内容弱化 NSFW（识别词条库 / 固定规则转化库 / AI 弱化） | `09` + `13` | **已移植**（B8-4：`core/nsfw.js` + `ui/nsfw.js`；共用 AI 钩子 `core/ai-hooks.js`） |
| 记忆遗忘机制 / 通用遗忘清扫 | `09` + `13` | **已移植**（B8-5：`core/forget.js` + `ui/forget.js`；`calcTimeDecay` 按注释语义修正 1970 前剧情日期） |
| 各类修复（记忆/原子/物品/概念/角色/场景/计划悬念合并去重） | `09` | **未移植**（`mergeDelta` 已有，修复用的提示词与合并策略未移植） |
| NSFW 弱化（词条库、固定规则转化库、AI 弱化） | `09/13` | **未移植** |
| 情节总结 / 分段总结自动管线 | `09/14` | 分段容器与哈希已移植，**自动总结管线未移植** |
| 传言演化（发酵/分裂/媒体/谱系） | `09/10` | 容器与归一化已移植，**演化管线未移植** |
| 遗忘清扫（时间衰减与清扫） | `09` | 容器与哈希已移植，**清扫管线未移植**（定时器钩子已就位） |

## 4. 进度打勾表（动作级）

B3 新增（+3 动作）：`summary`（分段批量）`abortAnalysis`（中断）`clearFloors`（清台账）；`extractNow`/`summaryFloor` 语义细化为逐楼/单楼

B1 已实现（12）：`tab` `close` `search` `edit` `editCancel` `save` `delete` `refresh` `summary` `summaryFloor` `extractNow` `inject`
B2 已实现（+15）：`add`/`addEntry` `addStateFor` `addChildScene` `closeEntry` `cancelEntry` `multiToggle` `selectAll` `selectNone` `bulkDelete` `atomToggleHidden` `atomPeek` `atomPeekClose` `searchClear`（含 V1 字段表驱动的编辑器）

后续批次实现时，在本表逐行打勾（每行一个 V1 动作名 → V2 实现位置 → 对应断言），确保 110 个动作**无一遗漏**：

```text
B2 ✅: addEntry add addStateFor addChildScene editEntry delEntry delete closeEntry cancelEntry editCancel
    multiToggle selectAll selectNone bulkDelete atomToggleHidden atomPeek atomPeekClose searchClear
    （字段表 kindFields/flattenSnapshot/deconstructEntry 已移植；编辑器按字段表渲染）
B3 ✅: summary summaryFloor extractNow clearFloors abortAnalysis（checkMode/checkRefresh = 注入自查，归 B5）
    （分段批量 = runAutoSummary；单楼 = analyzeFloor；中断 = abortExtract（段边界生效）；清台账 = clearProcessedFloors）
B4: （设定子页 14 组，非动作）
B5 ✅: relAddRow relRowDel relSetRow relSave relClearEntry relSweep relDropInferred relEdit relWho（按角色筛选）
    checkMode checkRefresh（注入自查两口径与刷新）—— 关系写入用 V1 `relSaveBox` 的 `{replace:true}` 口径
B6 ✅: promptSave promptResetOne promptResetAll promptGroupReset armorPresetImport promptMigrate（promptSig/migratePromptTemplates/migrateArmorPreset）
    —— 剩余：presetSave/Load/Delete（预设文件管理，依赖文件通道）留待 B9；`promptPreview` 经核实**V1 无此动作**（见 §6.1）
B7-1 ✅: snapCreate snapRestore snapDelete snapConsolidate snapshotClear
B7-2 ✅: storageStatusRefresh storageSync storageVerify syncLogRefresh syncLogClear
    —— 剩余：storageProbe/storageTest（V1 的多后端逐项探测，随「统一存储抽象」批次；V2 已用 `storageVerify` 覆盖校验/修复语义）
B8-1 ✅: clockEdit clockEditCancel clockManualSave clockManualClear clockPatrol
    —— 剩余：clockRepair clockRegexGen（AI 管线，B8-2）
B8-3 ✅: clockRegexGen clockRepair（AI 管线：三重校验 / 只改日期时间 + 安全闸门）
B8-4 ✅: nsfwSoften nsfwRuleApply nsfwRuleAdd nsfwRuleSave nsfwRuleDel nsfwRuleReset nsfwKwAdd nsfwKwSave nsfwKwDel nsfwKwReset
B8-5 ✅: 遗忘域（V1 无手动按钮：状态衰退由剧情日期推进调度、记忆遗忘由记忆写入调度、通用清扫在自动修复管线内执行）
    —— 另补 20 个 `switchField` 开关控件（feed/analyze/extract/forget/prompts/parallels/rumors/debug）
B8-6a ✅: 总览「repair」（🛠 自动修复）→ 第 1 段（JS 机械清理 · 零 AI）+ 频率/上限闸门 + 报告/日志
B8-6b ✅: 三段式修复（机械清理 → 候选筛选 → 窄契约 AI 修订）+ 提取失败自动修复
B8-6b+ ✅: 关联层机械维护（孤儿清扫/去重/改名/悬空引用/归一；keep·clean·public；AI 后复检并合并口径）—— 无 UI 按钮（关系表页「🧽 清扫孤儿关联」沿用 V1 同款 `sweepOrphanRelLinks`）
B8-7-a ✅: atomCompactNow atomMergeSummary plotSegmentSummarySel clearPlotSegments
    —— 情节总结（半自动聚合 + 手动多选合并，原文保留并隐藏）+ 情节分段总结（`### 时间范围` 归档，不注入、只增不减）；
       另含 `summary` 的 `data-ftt-summary=plotSegments` 分流与情节页子标签动作 `atomSub`
B8-6c+ ✅ 全部完成：关联维护 ✅ B8-6b+、记忆聚类修复 ✅ B8-6c-1、概念+场景修复 ✅ B8-6c-2、物品+角色档案修复 ✅ B8-6c-3、状态修复+计划悬念修复 ✅ B8-6c-4。
  **注**：V1 的 7 个专项修复动作（`memoryRepair`/`conceptRepair`/`sceneRepair`/`itemRepair`/`characterRepair`/`stateRepair`/`planSuspRepair`）已全部交付；**V1 并无 `atomRepair` 动作**（原子/情节维度由通用三段式 `repair` 覆盖，B8-6b 已交付）。
    memoryRepair atomRepair itemRepair conceptRepair characterRepair sceneRepair planSuspRepair repair
    nsfwSoften nsfwKwAdd/Del/Save/Reset nsfwRuleAdd/Del/Save/Reset/Apply
    parallelAdvance parallelAdvanceAll parallelWeaveNow promoteParallel rumorEvolve rumorAdvance clearRumors forgetSweep
B9-c ✅: rxScanTags rxAddTag rxScanClear（投喂标签自动分析：扫描最新正文结构 → 一键收录白/黑名单 + 清空结果）
    curTrackPick curTrackClose curTrackToggle curTrackClear（货币「👥 指定角色」标定：选择器 / 标定胶囊 / 清空）
B9: export import dbgClear aboutClearCache aboutReload apiModels apiTest savecfg reset
```

## 5. 样式对齐口径

- V1 的 527 行 CSS（437 处 `#ftt-panel` 规则）**逐字并入** V2 `style.css`（见文件内「逐字移植自 V1」注释段）；
- V2 渲染**同名同层级的 DOM**（`#ftt-panel > .ftt-modal > .ftt-modal-head/.ftt-tabs/.ftt-body`，行/按钮/编辑器类名一致：`.ftt-item` `.ftt-badge` `.ftt-btn` `.ftt-editor` `.ftt-field` `.ftt-empty` `.ftt-floor-btn` …），
  因此 V1 样式**无需改写即生效**；
- 新增页面沿用 V1 既有类名；确需新增样式时追加在 V2 自有段（文件末尾之前），不与 V1 段混写；
- 门禁 `scripts/check-docs.js` 只校验文档；样式无自动比对，故 B1 测试断言「CSS 含关键类且 `#ftt-panel` 规则 ≥300 条」防误删。

## 6. 剩余动作清单（按批次 · 2026-09-26 队长审计）

口径说明：V1 共 **110** 个 `data-ftt-action`；V2 现有实现中「零命中」的旧名字里，**一部分已由改名后的等价动作覆盖**（不重复实现），下表只列**确认为真缺失**的项。
已判定为改名等价、不再实现的 V1 旧名：`saveEntry`/`delEntry`（→ `editEntry` + 表单保存 / `delete` / `bulkDelete`）、`snapshotNow`/`snapshotRestore`（→ `snapCreate`/`snapRestore`，另有 `snapDelete`/`snapConsolidate`）、
`import`/`export`（→ `exportState`/`importStateOpen`/`importStateApply`）、`promptResetAll` 等提示词动作（→ `promptSave`/`promptResetOne`/`promptResetAll`/`promptGroupReset`）、`armorPresetImportFile`（→ `armorPresetImport` + 文件输入）、`checkMode`/`check-update`、`atomToggleHidden`/`atomPeek`/`atomPeekClose`。

| 批次 | 真缺失动作 | 备注 | 证据 |
| --- | --- | --- | --- |
| **B8-6c-2 ✅** | `conceptRepair` `sceneRepair` | **已交付并接线**（v2.19.0）：概念修复（机械合并 → 聚类选组 → 窄契约 AI → 按编号精确应用）+ 场景修复（路径数组/字符串双形态、非法拒收） | `docs/P8s`；`concept-repair-golden` 20 项 + `scene-repair-golden` 14 项 + 冒烟 X1–X3 |
| **B8-6c-3 ✅** | `itemRepair` `characterRepair` | **已交付并接线**（v2.23.0）：物品修复（机械去重 + 低调用固定规则清理 + 标签/名称聚类 + 窄契约 AI 融合）+ 角色档案修复（AI 前全局机械处理 + 出生日期异常优先档 + 已去世跳过 + 点路径只填空 + 加强重试）；见 `docs/P8u-B8-6c-3物品与角色修复.md` | `core/item-repair.js`（408 行）· `core/character-repair.js`（800 行）· `ui/panel.js` 物品页/角色页按钮 + `itemRepair`/`characterRepair` 动作 · `index.js`/`devtools.js` 19 个 `FTT.*` 入口 · fixture `tests/fixtures/v1-golden-item-repair.json` / `v1-golden-character-repair.json`（真实 V1 v1.206 oracle，逐字节可复现）· 单测 `tests/unit/item-repair-golden.test.js`（18 项）/ `tests/unit/character-repair-golden.test.js`（18 项）· 冒烟 AA1–AA3 |
| **B8-6c-4 ✅** | `stateRepair` `planSuspRepair` | **已交付并接线**（v2.26.0）：状态修复（已去世固定规则 → 匹配角色/无档案整组删除 → 机械清理与字段规范化 → 最薄弱主体交 AI 按「主体+字段」精确应用）+ 计划悬念修复（悬念同正文机械去重 → 关联层维护 → 标签组聚类/孤例轮询 → AI 按编号合并·修订·了结·删除 + 计划了结/冗余合并）；见 `docs/P8v-B8-6c-4状态与计划悬念修复.md` | `core/state-repair.js` · `core/plan-repair.js` · `core/ingest.js`（新增导出 `stateSubjectSamePerson` / `mergeStateHistory`）· `ui/panel.js` 状态页「🔧 修复状态」/ 计划悬念页「🔧 修复计划/悬念」按钮 + `stateRepair`/`planSuspRepair` 动作 · `index.js`/`devtools.js` 16 个 `FTT.*` 入口 · fixture `tests/fixtures/v1-golden-state-repair.json` / `v1-golden-plan-repair.json`（真实 V1 v1.206 oracle，逐字节可复现）· 单测 `tests/unit/state-repair-golden.test.js`（24 项）/ `tests/unit/plan-repair-golden.test.js`（18 项）· 冒烟 AB1–AB3 |
| **B8-7-a ✅** | `atomCompactNow` `atomMergeSummary` `plotSegmentSummarySel` `clearPlotSegments` | **已交付并接线**（v2.27.0）：情节总结（半自动聚合早期情节 + 手动多选合并为一条「A~B 总结」情节；原文保留并隐藏、不删除不记墓碑）+ 情节分段总结（打包 → AI 拆段 → `### 时间范围` 归档；只增不减、**产物不注入**、唯一消失途径是手动删除）；见 `docs/P8w-B8-7情节总结与分段总结.md` | `core/atom-compact.js`（495 行）· `core/plot-segment.js`（365 行）· `ui/panel.js` 情节页双子标签（📜 情节列表 / 🧩 分段总结）+ 多选「🧷 情节总结（N）」「🧩 分段总结（N）」+ 分段页「🧩 生成分段总结」「🧹 清理分段」+ 设定 → 提示词「早期情节压缩」节「🧷 立即聚合早期情节」· 动作 `atomCompactNow`/`atomMergeSummary`/`plotSegmentSummarySel`/`clearPlotSegments`（+ `summary` 的 `data-ftt-summary="plotSegments"` 分流、`atomSub` 子标签动作）· `index.js`/`devtools.js` 33 个 `FTT.*` 入口 · fixture `tests/fixtures/v1-golden-atom-compact.json` / `v1-golden-plot-segment.json`（真实 V1 v1.206 oracle，`Date.now` 固定、逐字节可复现）· 单测 `tests/unit/atom-compact-golden.test.js`（19 项）/ `tests/unit/plot-segment-golden.test.js`（17 项）· 冒烟 AC1–AC3 |
| **B8-7 ✅** | `rumorEvolve` `clearRumors` | 传言演化**已交付并接线**（v2.20.0 内核 + v2.21.0 界面/动作/FTT 入口）；注：V1 无 `rumorAdvance` 动作（那是内部函数 `rumorTickAdvance`），已从清单移除 |
| **B8-7-b ✅** | `parallelWeaveNow` `parallelAdvance` `parallelAdvanceAll` `promoteParallel` | **已交付并接线**（v2.28.0）：平行世界推演（交织管线：被动调度 1.8s + 忙位 2.5s 排队重试 + 楼层间隔闸门 + 输入签名去重 + 关键词命中优先更新 + `mergeDelta` 落库 + 推演后顺带衰退调度）+ 平行事件推进（单条/全部，按 `ADVANCE_CHUNK=6` 分批，`parallelExpired` 过滤，按 id 精确应用）+ 转正为情节（生成/更新情节原子后**自动移除**平行记录并留 id 墓碑；`prunePromotedParallels` 清理历史软标记）；见 `docs/P8x-B8-7平行推演与转正.md` | `core/parallel.js`（544 行）· `ui/panel.js` 总览「🧭 推演世界」（紧贴「📤 提取记忆」右侧）+ 平行页顶栏「🚀 全部推进」与行内「🚀 推进 / ⬆ 转正为情节」（过期不显示 🚀、已转正不显示 ⬆）+ 4 个同名动作分支 + 确认闸门 · `index.js` 取文钩子（`setParallelTextHooks` → `host/floors.js`）/ 启动 `prunePromotedParallels` / 15 个 `FTT.*` 入口 · `devtools.js` 同 15 个入口（`typeof` 守卫）· fixture `tests/fixtures/v1-golden-parallel.json`（真实 V1 v1.206 oracle，5 组 30 例，`Date.now` 固定、逐字节可复现）· 单测 `tests/unit/parallel-golden.test.js`（33 项）· 冒烟 AD1–AD3 |
| **B8-7 ✅** | `worldbookRefresh` | 世界书存储单向镜像**已交付并接线**（v2.20.0 内核 + v2.21.0 设定页「📚 刷新世界书列表」按钮/动作/FTT 入口/下拉动态填充） |
| **B10-a ✅ / B9 收窄** | `presetSave` `presetLoad` `presetDelete` `apiTest` `apiModels` | **已交付并接线**（v2.35.0，见 `docs/P10a`）：API 分组预设三动作 + 🧪 连通性测试 + 📦 获取模型（三通道：跟随酒馆 / 酒馆连接配置 / 自建连接直连） |
| B9（收窄） | `savecfg` `testLayer` | `savecfg`：V1 的批量「保存设置」在 V2 无对应物（控件即时落盘，判定不变）；`testLayer`：**三层提取分别测试**，属未实现的**向量检索层**（§6.3） |
| **B9-a ✅** | `dbgClear` `aboutClearCache` `aboutReload` | **已交付并接线**（v2.29.0）：调试页（V1 同款查看器：类别标签/摘要/计数/逐条 `details` + 「🗑 清空日志」）+ 关于页（版本清单**按扩展目录**读取、成功/缓存/失败三态、倒序版本条目、`🔄 重新获取` / `🧹 清除本地缓存`）；见 `docs/P8z-B9调试页与关于页.md` | `ui/debug.js`（新建）· `ui/about.js`（新建）· `ui/settings-pages.js` 的 `debug`/`about` 分支 + `PENDING_NOTE.debug` 移除 · `ui/panel.js` 动作分发（`DEBUG_ACTIONS`/`ABOUT_ACTIONS`）· `index.js` 启动 `wireDebugLog()` + 18 个 `FTT.*` 入口 · `devtools.js` 同 18 个（`typeof` 守卫）· fixture `tests/fixtures/v1-golden-debug-log.json` / `v1-golden-about.json`（真实 V1 v1.206 oracle，逐字节可复现）· 单测 `tests/unit/debug-log-golden.test.js`（16 项）/ `tests/unit/about-golden.test.js`（15 项）· 冒烟 AE1–AE2 |
| **B9-a ✅** | `reset`（清空当前角色记忆） | **已交付并接线**（v2.29.0）：数据管理页「🗑 清空当前角色记忆」（文案与 V1 逐字一致；确认文案逐字一致；无对话框不执行）+ `adapters/store.js#resetState`（抑制整批墓碑 → 空容器 → 保存 → 复位索引基线） | `adapters/store.js#resetState` · `ui/panel.js` 的 `reset` 分支 · `devtools.js`/`index.js` 的 `FTT.resetState()` · fixture `tests/fixtures/v1-golden-reset.json`（真实 V1 v1.206 oracle）· 单测 `tests/unit/store-chat.test.js` S8–S14（7 项）· 冒烟 AE3 |
| **B9-c ✅** | `rxScanTags` `rxAddTag` `rxScanClear` | **已交付并接线**（v2.31.0）：投喂标签自动分析 —— 扫描**最近一条 AI 正文**（回溯 60 楼；跳过隐藏楼/用户楼/非 assistant 楼/空白楼）→ 列出 HTML 标签（成对优先）与行内标记（各取前 30）→ 点「＋白 / ＋黑」**一键收录**（大小写不敏感自动排重、另一侧名单提示）→ `rxScanClear` 清空结果；含 V1 同款设定页「投喂标签自动分析」节与**投喂白/黑名单文本域**（保存时整表排重）；见 `docs/P9b-B9投喂标签与货币追踪.md` | `ui/feed-scan.js`（新建：`latestAiFloorInfo` / `rxAnalyzeLatestText` / `rxNormTag` / `rxDedupeTagList` / `rxPushFeedTag` / `rxTagScanHtml` / `feedScanSectionHtml` / `feedTagListSectionsHtml` / `feedScanAction` / `FEED_SCAN_ACTIONS`）· `ui/settings-pages.js` 的 `feed` 分支 · `ui/panel.js` 的 `FEED_SCAN_ACTIONS` 分支 + change 委托整表排重 · `index.js`/`devtools.js` 10 个 `FTT.*` 入口（`typeof` 守卫）· fixture `tests/fixtures/v1-golden-feed-scan.json`（真实 V1 v1.206 oracle：动作类经 `openPanel()` + `panel.listeners.click` **真实点击委托**驱动 `handleAction`，`rxTagScanHtml` 5 态与设定节静态片段**逐字节**；生成器已入库 `tests/fixtures/gen-v1-golden-feed-scan.cjs`，两次运行逐字节一致）· 单测 `tests/unit/feed-scan-golden.test.js`（17 项 = R1–R10 V1 逐项比对 + V1–V7 V2 编排/接线）· 冒烟 AG1–AG3 |
| **B9-c ✅** | `curTrackPick` `curTrackToggle` `curTrackClear` `curTrackClose` | **已交付并接线**（v2.31.0）：货币页「👥 指定角色（N）」标定选择器（**只从角色档案点名**；行内 `➕`/`✅` 带 V1 同款 title、`已标定` 角标、`关闭`；搜索词走页面搜索词槽）+ 标定胶囊（`⭐ 已标定跟踪：` 内 `✖` 直接取消）+ `✖ 清空标定`（有标定才显）+ 统计胶囊 `已标定 K 名` 角标；标定名单供 `currenciesTracked` 提示词段与货币账本使用（既有内核）；见 `docs/P9b-B9投喂标签与货币追踪.md` | `ui/panel.js` 的 `currencyTopHtml` / `currencyStatText` / `currencyTrackButtons` / `currencyPickPanelHtml` + `curTrackPick`/`curTrackClose`/`curTrackToggle`/`curTrackClear` 四个分支 · `core/model/money.js` 新增 `trackPickState` / `setTrackPick`（V1 `currencyTrackPicking` 的等价物）· `index.js`/`devtools.js` 10 个 `FTT.*` 入口（`typeof` 守卫）· fixture `tests/fixtures/v1-golden-cur-track.json`（真实 V1 v1.206 oracle：动作类经**真实点击委托**驱动，货币页 HTML 取自 `panel.innerHTML`；名单 19 步含 V1「简称包含」不一致怪癖；生成器已入库 `tests/fixtures/gen-v1-golden-cur-track.cjs`）· 单测 `tests/unit/cur-track-golden.test.js`（13 项 = R1–R8 V1 逐项比对 + V1–V5 V2 编排/接线）· 冒烟 AG1–AG3 |
| **B9-d ✅** | 条目瘦身 + gzip 传输（`.json.gz`） | **已交付并接线**（v2.32.0）：瘦身（`core/slim.js`，V1 `slimEntryForStorage`/`slimDataForStorage`/`slimSnapshotStoreForStorage` 等**逐字移植**：不写空值/默认值、同义字段全等才丢、`extra` 只留独有槽位、快照内容剥离为 `snapIndex`）+ gzip 写入（`adapters/gzip.js` + `uploadStateFileGz`：先 gzip 再 base64，失败/通道不支持**自动回退明文**）+ 读取侧**按内容魔数**（`1f 8b`）自动识别（`readStateFileAuto`）。**默认安全**：`cfg.storage.stateFileSlim` / `stateFileGzip` 均默认 `false`（关闭时写入名/内容/请求序列与 B7-2 完全一致）；开启后写 `.json.gz`，**明文旧文件始终可读**；**不删除**被 gz 取代的明文文件（V1 会删本会话读过的）。见 `docs/P9c-B9瘦身gzip与同步分歧选择.md` | `core/slim.js`（新建）· `adapters/gzip.js`（新建）· `adapters/user-file.js`（`readStateFileAuto`/`uploadStateFileGz`/`FILE_EXT_GZ`）· `adapters/sync.js`（`slimFileEnvelope`/`stateFileWriteName`/`snapshotFileWriteName`/`uploadContentMaybeGz`/`writeStateFileContent`/`slimGzipInfo`）· `adapters/store.js`（保存流水线步骤⑥与载入改走同一瘦身/gzip 通道）· `ui/sync.js#slimGzipInfoHtml` · `core/config.js` 两个默认关闭的开关 · `index.js`/`devtools.js` 14 个 `FTT.*` 入口（`typeof` 守卫）· fixture `tests/fixtures/v1-golden-slim-gzip.json`（真实 V1 v1.206 oracle：写入路径经桩 fetch 捕获 base64 → 解字节 → 魔数/结构比对；生成器已入库）· 单测 `tests/unit/slim-gzip-golden.test.js`（17 项 = R1–R8 V1 逐项比对 + B1–B5 **读写向后兼容** + V1–V4 V2 编排/接线）· 冒烟 AH1–AH2 |
| **B9-d ✅** | `syncPickLocal` `syncPickRemote` | **已交付并接线**（v2.32.0）：跨端对账遇**分歧**（两端各有独有/冲突且无端是超集）→ **不静默合并**，暂存最后一份对端信封并在存储页渲染 V1 同款分歧横幅（标题/统计/按钮文案逐字；两按钮**无 `title`**，V1 原文如此）；「保留本端（覆盖对端）」= `storageWriteAll` 本端覆盖对端；「采用对端（整体替换）」= `applyRemoteReplaceState`（长任务在途**降级为并集合并**、失败则放弃并如实警示）；两者都写同步日志 `action='分歧选择'`（mode/note 与 V1 逐字）。**默认安全**：V2 的自动路径写入点唯一为「保存后镜像」（V1 `crossPullPolicy` 的多触发源 V2 不存在），`立即同步` 与 V1 一致仍走原子融合不产生待选；见 `docs/P9c-B9瘦身gzip与同步分歧选择.md` | `adapters/sync.js`（`crossPendingGet`/`crossPendingClear`/`crossPendingView`/`applyRemoteReplaceState`/`stashDivergence`）· `ui/sync.js`（`divergenceBannerHtml` + `syncAction` 两分支；`SYNC_ACTIONS` 6 → 8）· `index.js`/`devtools.js` 10 个 `FTT.*` 入口（`typeof` 守卫：`crossComputeInfo`/`crossPendingGet`/`crossPendingView`/`crossPendingClear`/`applyRemoteReplaceState`/`adoptRemoteEnvelope`/`crossPullPolicy`/`storageEnvelope`/`storageHash`/`storageEnvValid`）· fixture `tests/fixtures/v1-golden-sync-pick.json`（真实 V1 v1.206 oracle：动作类经 `openPanel()` + `panel.listeners.click` **真实点击委托**驱动 `handleAction`；横幅由 V1 `renderStorageStatus()` 真实写入；生成器已入库 `tests/fixtures/gen-v1-golden-sync-pick.cjs`，两次运行逐字节一致）· 单测 `tests/unit/sync-pick-golden.test.js`（13 项 = R1–R10 V1 逐项比对 + V1–V4 V2 编排/接线）· 冒烟 AH2–AH3 |
| ✅ 已完成 | `snapshotInspect` | v2.19.0 补齐：`ui/snapshots.js` 行内展开（V1 `alert` 的等价呈现；清单口径与 V1 一致）+ `🔍` 按钮 |
| ✅ 改名等价 | `relEditorAddRow` | V2 `relAddRow`（关系表加空行） |
| **B9-b ✅** | `relJump` `relGoto` `relPick` `relPickAdd` `relPickClose` | **已交付并接线**（v2.30.0）：条目行「🔗 关联（N）」↔ 关系表**双向定位跳转**（`relJump` 切到条目所在页 + 该维度子标签置「关系表」+ 定位提示 + 目标条目即使暂无关联也列出；`relGoto` 打开条目所在页并把该页搜索词设为条目标题）+「👥 选角色」选择器（**只从角色档案点名**，➕ 追加草稿行、仍要点「💾 保存关联」落库；重复角色提示、同条目再点收起）；见 `docs/P9a-B9关系表定位与选角色.md` | `ui/rel-table.js` 新增 `relEntryTitle` / `relFindEntryId` / `relKnownNames` / `relPickState` / `setRelPick` / `relFilterState` / `setRelFilter` / `relJump` / `relGoto` / `relPickAppendRow` / `relPickPanelHtml` / `relPickKey` / `relPickingOf` / `REL_TAB_OF` / `relDimLabelOf` / `relIsRelDim`，`relTableHtml(dim,refId,{editor})` 增「👥 选角色」面板与 `relSave` 关选择器 · `ui/panel.js` 条目行 `relJumpBtn` + 关系总览「↗ 打开条目 / 👥 选角色 / 💾 保存关联 / 🔗 编辑」+ 6 个动作分支（置于 `rel*` 兜底分支之前）+ DOM 委托 `data-name`/`data-editor`/`data-ftt-rel-idx` · `index.js`/`devtools.js` 16 个 `FTT.*` 入口（`typeof` 守卫）· fixture `tests/fixtures/v1-golden-rel-nav.json`（真实 V1 v1.206 oracle：动作类经 `openPanel()` + `panel.listeners.click` **真实点击委托**驱动 `handleAction`，并原样保存 V1 源码动作序片段；两次运行逐字节一致）· 单测 `tests/unit/rel-nav-golden.test.js`（19 项 = R1–R12 V1 逐项比对 + V1–V7 V2 编排/接线）· 冒烟 AF1–AF3 |
| **B9-b ✅（收窄）** | `relClearFilter` | **已交付**：V2 收窄为「清空**角色筛选** + 跳转定位 + 选角色态」—— `relFilterState().dim` 仅保留字段（供 V1 口径对照与诊断），**不参与过滤**：V2 关系表按**分页**隔离维度（`ui/panel.js#REL_TABDS`，一页只显示一个维度），V1 的「维度筛选下拉」在 V2 设计下不适用（判定见 §6 首行与 `docs/P8f-B5关系表与注入自查.md`）。另两处必要偏离：V1 `relJump` **恒切「记忆」页**（靠维度筛选过滤），V2 切到**条目所在页**；`relPickAppendRow` 首参由 DOM 容器改为 `dim + refId`（「容器缺失」态 = 条目引用为空 / 非关联维度 / 空名 / 关联层关闭，返回三态 `true` / `'dup'` / `false` 不变） | 单测 R5/R10 显式断言三态与「dim 不参与过滤」；冒烟 AF1/AF2 断言状态迁移与提示文案；fixture 含 V1 `relClearFilter` 源码片段与四维动作序 |
| **✅ 已完成** | `clearPlans` `clearSuspense` | v2.22.0 补齐：计划悬念页「🧹 清理计划」「🧹 清理悬念」（各自库非空才显，`title` 与 V1 逐字一致；**不弹确认**）+ 动作写删除墓碑并清空；清空后按钮隐藏 |

### 6.1 V2 不适用（设计差异 · 明确不做，避免做假控件/假动作）

| V1 动作 | 不适用原因 |
| --- | --- |
| ~~`presetSave` `presetLoad` `presetDelete`~~ | **❌ 该判定已于 v2.35.0 推翻** —— 酒馆向扩展暴露了官方通道 `ConnectionManagerRequestService`（`st-context.js:292`，`shared.js:419` 的 `sendRequest` 支持 `overridePayload` 覆盖 `temperature`/`top_p`/`max_tokens`），故 API 一族**可以真实实现**；现已实现，见 `docs/P10a-API页与按用途渠道对齐.md` 与 §6.4 |
| ~~`apiTest` `apiModels`~~ | 同上（v2.35.0 已实现：`host/api-channel.js#probeTarget` / `#fetchModels`，端点拼接、`Bearer` 鉴权、报错文案与 V1 逐字） |
| ~~`apiTemperature` `apiMaxTokens` `apiTopP`~~ | 同上（v2.35.0 已实现：`profile`/`direct` 通道直传；`host` 通道 `max_tokens` 经宿主 `responseLength`，`temperature`/`top_p` 在 `host` 通道**确实无法覆盖**并已如实标注） |
| ~~按用途 API 预设（`activeApiPreset`/`parallelApiPreset` 选择器）~~ | 同上（v2.35.0 已实现；`kwApiPreset`/`memApiPreset` 改判为「属向量检索层，未实现」→ §6.3） |
| ~~`dimensionPresets`（各维度单独选预设）~~ | 同上（v2.35.0 已实现：逐维度下拉 + `host/extract.js#runSeparateGroup` 按维度解析 target；P9d 的「宿主签名无 preset 参数」取证仍成立，但那是 `generateRaw` 的签名，**不是酒馆扩展通道的边界**） |
| `savecfg` | V1 是「一次性把所有设定页控件写回并落盘」（`settingsApplyAll`）；V2 每个控件**即时写回内存 + 持久化**（`applySettingsControl` → `saveKernelCfg`），无「批量保存」概念 |
| `promptPreview` | **V1 无此动作**（核实：`grep -c "promptPreview" src/FTT记忆组件-v1.206.js` → `0`，v1.204/v1.205 同；`grep -rin "promptpreview" src/`（含 `modules/`）无输出；V1 全部 `data-ftt-action` 值中无任何含 `preview` 的动作、也无 `function …preview…` 命名函数）→ 属总表早期清单的笔误项，**无对应物**。V2 的提示词/注入预览能力已由 B5 的「注入自查」（`ui/inject-check.js#injectCheckPanelHtml` / `injectCheckAction` / `injectCheckStats`，含约束段原样预览与未召回原因）覆盖，不重复实现 |

### 6.2 两个实质缺口（B9-a 已闭环）

| 缺口 | 证据 | 处置 |
| --- | --- | --- |
| **调试日志存储缺失** | 昔日 `host/chat.js` 把内核 `dbgLog` 接成 **空实现**（`dbgLog: () => undefined`），全仓无日志环形缓冲/查看器 | **已闭环**：v2.23.0 落地内核 `core/debug-log.js` + `adapters/debug-log.js`（localStorage 接线）；B9-a 补**查看器**（`ui/debug.js`）+ `dbgClear` 动作 + `FTT.*` 入口，见 `docs/P8z-B9调试页与关于页.md` |
| **清空当前角色记忆（`reset`）缺失** | V1 `case 'reset'` → `confirm(...)` + `resetState()`（清空该角色全部 FTT 数据）；V2 曾全仓无 `resetState` 等价入口 | **已闭环**：`adapters/store.js#resetState`（V1 逐步口径 + 抑制整批墓碑）+ 数据管理页按钮与**逐字确认文案** + 冒烟 AE3，见 `docs/P8z-B9调试页与关于页.md` |

> 数据面板 13 个分页（`overview/atoms/states/snapshots/memories/items/currencies/rumors/plans/scenes/concepts/parallels/settings`）与 V1 `TABS` **同名同序**，已核对一致。

### 6.3 向量检索层（未实现 · 明确登记）

V1 的**三层提取结构**（`core/config.js` 的键注释：第一层向量检索 `useVector` / 第二层浏览器 JS 抽取 `jsExtractEnabled` /
第三层 AI 分析 `memApi`）在 V2 **整体未实现**：全仓 `grep -rn "useVector" core/ host/ ui/ index.js` 仅命中默认值本身。

因此以下 V1 能力/配置**属该层，未实现**（**不放假控件**）：
`kwApiPreset`/`kwApiEnabled`+`kwApi`（AI 关键词提取，V1 12575 · 消费者 `extractKeywordsFromText`）·
`memApiPreset`/`memApiEnabled`+`memApi`（记忆分析发送，V1 12596 · 消费者 `analyzeMemorySend`）·
Embedding API / Rerank API（V1 25643/25646）· `testLayer`（三层分别测试，V1 27335 附近）。

已就位的基础设施（向量层批次可直接接线，无需再改）：`core/api-channel.js#resolveApiTarget` 支持
`purpose:'kw'|'mem'` 与内联 `kwApi`/`memApi`；`host/api-channel.js#probeTarget` 保留 `embedding`/`rerank` 两种 kind 的
V1 语义（端点拼接 / 请求体 / 报错文案）。

### 6.4 B10-a 已实现清单（v2.35.0）

见 `docs/P10a-API页与按用途渠道对齐.md`：API 子页两分节（API 分组（预设管理）+ API 设定（主 API 配置））+
「按用途渠道」分节；三通道（`host`/`profile`/`direct`）；参数生效矩阵；旧数据迁移口径；
新增入口 `FTT.apiChannelSummary/apiChannelAvailability/apiProfiles/apiTarget/apiPresetNames/apiPresetSave/apiPresetLoad/apiPresetDelete/apiTest/apiModels/apiSend`。

## 7. 会话交接（2026-09-26）

**已发布并推送的版本标签（每个都通过 post-tag `npm run gate` 与 `git archive` 解包复验）**：

| tag | 内容 | 门禁口径 |
| --- | --- | --- |
| v2.18.0 | B8-6c-1 相关组聚类修复（通用聚类引擎四域 spec + 记忆修复管道 + `retargetRelRefs`）+ 设定页 22 控件 | 单元 40 / 566，冒烟 99 |
| v2.19.0 | B8-6c-2 概念 + 场景修复；设定页「各大类单条字数上限」10 控件；快照内容查看 `snapshotInspect`；覆盖度审计 §6 | 单元 42 / 602，冒烟 102 |
| v2.20.0 | B8-7 传言演化引擎（615 行/35 导出，零 AI）+ 世界书单向镜像内核（core/host/adapters 三件套） | 单元 44 / 665，冒烟 102 |
| v2.21.0 | B8-7 接线：传言「🧪 立即演化 / 🧹 清理传言」、世界书「📚 刷新世界书列表」+ 下拉动态填充 + 24 个 `FTT.*` 入口 | 冒烟 105（Y1–Y3） |
| v2.22.0 | `clearPlans` / `clearSuspense`（V1 同款按钮与 title、写墓碑、清空后隐藏） | 冒烟 106（Z1） |
| v2.23.0 | B9 前置：调试日志环形缓冲（`dbgLog` 由空实现改为真实记录 + localStorage 接线） | 单元 45 / 670，冒烟 106 |

**进行中（未完成，勿当已交付）**：
- **B8-6c-3 物品修复 + 角色档案修复**：已按「先物品域、后角色域」拆半交付（物品域要求：`core/item-repair.js` + 按钮/动作 + fixture + 单测 + 冒烟 AA + 先跑一次 gate 回报）。若其回报时只有物品域完成，**只发物品域**并在 CHANGELOG 注明角色域未完成。

**批次收官（队长终审 · 2026-09-26）**：B8 高级域与 B9 全部批次均已交付并逐批发版 ——
B8-6c-1（v2.18.0）· B8-6c-2（v2.19.0）· B8-7 内核（v2.20.0）· B8-7 接线（v2.21.0）· 计划/悬念清理（v2.22.0）· B9 前置调试日志内核（v2.23.0）·
B8-6c-3（v2.24.0）· **测试完整性专项（v2.25.0，45 处假绿断言转正 + `/ftt-import apply` 真实缺陷修复）** · B8-6c-4（v2.26.0）· B8-7-a（v2.27.0）·
B8-7-b（v2.28.0）· B9-a（v2.29.0）· B9-b（v2.30.0）· B9-c（v2.31.0）· B9-d（v2.32.0）·
**P9d 独立分组抽取 + 被动调度接线（v2.32.0 之后，待发版）**。

**仍然存在的非动作项（明确登记，非"已对齐"）**：
1. **设定页「投喂世界书」节**（V1 `renderWorldbookSettings` / `data-ftt-wb-list`，属**世界书投喂**能力，与已交付的「世界书单向镜像」不同）——未实现，**未放假控件**。
2. ~~**被动调度未接线**~~ → **✅ 已闭环（P9d，v2.33.0）**：`host/extract.js` 已按 V1 的四个检查点接线 —— 单楼分析合并成功 → `scheduleParallelWeave({floorId}, jsExtractKeywords(text))` + 无条件 `scheduleAtomCompact()`（V1 15240/15242）；分段（统一路径）合并成功 → `scheduleParallelWeave(seg, jsExtractKeywords(segText))`（V1 15490）；批量收尾 `finally` → `scheduleAtomCompact()`（V1 15537）。真实抽取 → 1.8s/4s 排程由冒烟 **AI2/AI3**（注入定时器驱动）与单测 **W1–W6** 固化；V1 事实核验：这两个调度点都**不在** `adapters/store.js` 的保存函数里（故未改存储层）。
3. ~~**`dimensionGrouping`（V1「独立分组 / 各维度单独选预设并行请求」）**~~ → **✅ 已闭环（P9d，v2.33.0）**：`host/extract.js#runSummarySeparate / summaryDimGroups` 实现分组（生效维度各自单组 + 未启用维度合成「统一」组）与并行请求、逐组切片 `mergeDelta`、逐组计成败、成功组各触发被动推演；接入位置与 V1 一致（**只有**分段路径 `analyzeSegment`，单楼分析 V1 本无该分支）；设定页「分析记忆」页新增 V1 同款「独立分组」开关（代理键 `dimensionSeparate` → 真键 `cfg.dimensionGrouping`）；`core/parallel.js#jsExtractKeywords` 逐字移植。黄金样本 `tests/fixtures/v1-golden-separate-dim.json` + 单测 `tests/unit/separate-dim-golden.test.js`（17 项）+ 冒烟 AI1–AI3；按维度选预设**不适用**（见 §6.1）；细节见 `docs/P9d-独立分组抽取与被动调度接线.md`。
4. **B9-d 未实现项**：`cleanupSupersededPlain`（明文旧文件清理）、`fileNameRemember`/`fileNameKnownNames`（写入名历史）、`syncBusy` 同步占用 UI、TauriTavern 原生通道下的 gzip、同步 `dbgLog` 留痕。
5. **界面细节**：`relFillBatch` 界面入口（V1 亦已移除，仅程序化接口）、分段子页搜索/排序控件、货币选择器的 4 下拉筛选条（V2 用单输入框）。
6. **`promptPreview`**：经核实 **V1 并无此动作**（v1.204/v1.205/v1.206 及 `modules/` 全分片 grep 均为 0 命中），V2 的提示词/注入预览由 B5「注入自查」覆盖 —— 详见 §6.1。
7. **B9-d 默认口径**：`cfg.storage.stateFileSlim` / `stateFileGzip` **默认 `false`**（关闭时与 v2.31.0 逐字节一致；开启后与 V1 同口径）。若要严格对齐 V1 的「恒开」默认，可把两开关置 `true`。
8. **向量检索层（三层提取的第一层与配套配置）**：V1 有、V2 **完全未实现** —— `useVector`/Embedding/Rerank、
   AI 关键词提取（`extractKeywordsFromText`）、记忆分析发送（`analyzeMemorySend`）、`testLayer` 三层测试；
   对应设定控件（`kwApiPreset`/`memApiPreset`/Embedding/Rerank API 块）**不实现、不放假控件**，见 §6.3。
   注：`host/api-channel.js#probeTarget` 已按 V1 语义预留 `embedding`/`rerank` kind，向量层批次可直接复用。
9. **V1 原生怪癖**：各批已**刻意原样保留**并在对应 `docs/P8*`/`docs/P9*` 文档逐条登记（不做"顺手修正"）；
   经队长裁决**明确修正**的 V1 故障共 3 处：v2.29.0 两处（关于页重试风暴、`aboutLoadJson` 在途标志污染）+
   **v2.35.0 一处**（`apiTemperature`/`apiMaxTokens`/`apiTopP` 被 `settingsApplyAll` 的
   `else if (!key.startsWith('api'))` 兜底排除 → **永不落 cfg**，v1.206 **26746**；V2 改为即时写回并在
   `tests/unit/api-channel-golden.test.js` 断言差异，见 `docs/P10a` §4-8）。

**方法论（后续批次请沿用）**：每批用**真实 V1 插件 v1.206** 作 oracle（`tests/unit/helpers.js#loadPlugin`）生成 fixture，并由**队长独立重跑 oracle** 验证「非手写、可逐字节复现」；子代理「停笔后再报告」，禁止与发布并行写同一批文件；发版前发现缺陷必须如实记录在 CHANGELOG（本会话已记录 X3 标签并集期望错误的修复过程）。

### 7.1 参照物口径澄清（2026-09-26 · 队长裁决）

V1 仓库 `src/` 下同时存在 `FTT记忆组件-v1.204.js` / `-v1.205.js` / `-v1.206.js`（后两者同 09-24 生成）。
V1 自带测试基建 `tests/unit/helpers.js#findPluginSrc` **取版本号最大者**，故：

- **所有批次的 oracle（含所有 fixture）均使用 `-v1.206.js`**（fixture 内 `meta.v1Version='v1.206'` 自证）；
- 队长此前给出的**行号引用**多取自 `-v1.205.js`（内容与 v1.206 近乎一致，个别术语有差异，例如情节总结的产物在 v1.205 为「移除+替换」、在 v1.206 为「**隐藏**」）；
- **后续批次请以 `-v1.206.js` 为引用与对照文件**（grep 文案/title 时用 v1.206，行为以 oracle 实测为准）。
